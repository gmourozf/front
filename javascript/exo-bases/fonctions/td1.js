// 1   % correspond au calcul du modulo

function prenom(prenom) {

    console.log(`Mon prenom est ${prenom} `)

}

let monPrenom = "toto";
prenom(monPrenom);


// exercice 1 fonction de multiplication.

const a = 5,
    b = 6;


function multiplier(valeur1, valeur2) {
    return valeur1 * valeur2;
}

let calcul = multiplier(a, b);
console.log(calcul);